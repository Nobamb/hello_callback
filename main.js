// 두 변수에 대해 더하는 함수를 생성한다.
const plus = (a,b) => {

    return a + b

}

// 두 변수에 대해 빼는 함수를 생성한다.
const minus = (a,b) => {

    return a - b

}

// 두 변수에 대해 곱하는 함수를 생성한다.
const multiple = (a,b)=> {

    return a * b

}